// RUN: %forge %s --emit-mlir | %FileCheck %s

// CHECK: %c1_i64 = arith.constant 1 : i64
// CHECK: %c2_i64 = arith.constant 2 : i64
// CHECK: %[[X:.*]] = arith.addi %c1_i64, %c2_i64
// CHECK: %c3_i64 = arith.constant 3 : i64
// CHECK: arith.addi %[[X]], %c3_i64

let x = 1 + 2;
let y = x + 3;
y
